# ESP-IDF build artifacts
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
.venv/
venv/

# ML artifacts (regenerate via scripts)
*.tflite
*.h5
*.keras

# Experiment outputs (regenerate via scripts/run_experiment.sh).
# The reference-paper-runs subdir is explicitly kept — those are the
# archived logs that reproduce every table in the paper.
logs/*
!logs/reference_paper_runs/
results/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~
