# ESP-IDF build artifacts
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
.venv/
venv/

# ML artifacts (regenerate via scripts)
*.tflite
*.h5
*.keras

# Experiment outputs. We ship the derived results (results/) and the raw serial
# logs (logs/) that back every table in the paper, so readers can reproduce
# every numerical claim without the hardware. Large instrumented serial
# captures are gzipped. Only transient scratch files are ignored.
*.tmp
logs/**/*.tmp

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~
